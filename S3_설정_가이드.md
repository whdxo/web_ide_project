# AWS S3 설정 가이드

## ✅ 완료된 작업

### 1. 백엔드 수정
- ✅ `FileContentController.java` - 경로 중복 버그 수정
  - `/api/files/api/files/{fileId}/upload-url` → `/api/files/{fileId}/upload-url`
  - `/api/files/api/files/{fileId}/content-url` → `/api/files/{fileId}/content-url`

### 2. 프론트엔드 수정
- ✅ `file.types.ts` - S3 관련 타입 추가
  - `UploadUrlResponse` 타입 정의
  - `DownloadUrlResponse` 타입 정의

- ✅ `fileApi.ts` - S3 방식으로 완전 전환
  - `getFileContent()` - S3 다운로드 방식
  - `saveFileContent()` - S3 업로드 방식

### 3. 백엔드 재시작
- ✅ 포트 8080에서 정상 실행 중

---

## 🔧 남은 작업: AWS S3 설정

### 현재 상태
```yaml
# application-local.yml
aws:
  s3:
    bucket: editus-dev-bucket  # ← 버킷 이름만 설정됨
    region: ap-northeast-2     # ← 리전만 설정됨
```

**문제**: AWS Credentials (Access Key, Secret Key)가 없어서 S3 접근 불가

---

## 📋 AWS S3 설정 방법

### 옵션 1: AWS CLI 설정 (권장)

```bash
# AWS CLI 설치 (Mac)
brew install awscli

# AWS Credentials 설정
aws configure

# 입력 정보:
# AWS Access Key ID: [Your Access Key]
# AWS Secret Access Key: [Your Secret Key]
# Default region name: ap-northeast-2
# Default output format: json
```

설정 파일 위치: `~/.aws/credentials`

```ini
[default]
aws_access_key_id = YOUR_ACCESS_KEY
aws_secret_access_key = YOUR_SECRET_KEY
```

### 옵션 2: 환경 변수 설정

```bash
export AWS_ACCESS_KEY_ID="your-access-key"
export AWS_SECRET_ACCESS_KEY="your-secret-key"
export AWS_REGION="ap-northeast-2"
```

**백엔드 실행 시 함께 설정**:
```bash
cd /Users/jongtae/Desktop/web_ide/backend

AWS_ACCESS_KEY_ID="your-access-key" \
AWS_SECRET_ACCESS_KEY="your-secret-key" \
AWS_REGION="ap-northeast-2" \
SPRING_PROFILES_ACTIVE=local \
JUDGE0_API_KEY="..." \
OPENAI_API_KEY="..." \
./gradlew bootRun
```

### 옵션 3: application-local.yml에 직접 설정 (비권장)

```yaml
aws:
  s3:
    bucket: editus-dev-bucket
    region: ap-northeast-2
  credentials:
    access-key: YOUR_ACCESS_KEY
    secret-key: YOUR_SECRET_KEY
```

⚠️ **주의**: credentials를 코드에 직접 넣으면 보안 위험! Git에 올리지 말 것!

---

## 🪣 S3 버킷 생성 방법

### 1. AWS Console에서 생성

1. AWS Management Console → S3
2. "버킷 만들기" 클릭
3. 버킷 이름: `editus-dev-bucket`
4. 리전: `아시아 태평양(서울) ap-northeast-2`
5. "퍼블릭 액세스 차단" 설정:
   - ✅ 모든 퍼블릭 액세스 차단 (Presigned URL은 작동함)
6. 버킷 생성

### 2. IAM 사용자 생성 및 권한 부여

1. AWS Console → IAM → 사용자
2. "사용자 추가" 클릭
3. 사용자 이름: `editus-dev-user`
4. 권한 설정:
   - "직접 정책 연결" 선택
   - `AmazonS3FullAccess` 정책 연결
   - 또는 최소 권한 정책:

```json
{
  "Version": "2012-10-17",
  "Statement": [
    {
      "Effect": "Allow",
      "Action": [
        "s3:PutObject",
        "s3:GetObject",
        "s3:DeleteObject"
      ],
      "Resource": "arn:aws:s3:::editus-dev-bucket/*"
    },
    {
      "Effect": "Allow",
      "Action": [
        "s3:ListBucket"
      ],
      "Resource": "arn:aws:s3:::editus-dev-bucket"
    }
  ]
}
```

5. "보안 자격 증명" 탭 → "액세스 키 만들기"
6. Access Key와 Secret Key 복사 (한 번만 표시됨!)

---

## 🧪 테스트 방법

### 1. S3 연결 테스트

백엔드가 실행 중인 상태에서:

```bash
# 파일 생성 API 호출
curl -X POST http://localhost:8080/api/projects/1/files \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -d '{"name": "test.txt", "folderId": null, "language": "plaintext"}'

# 응답 예시:
# {"id": 123, "name": "test.txt", ...}

# 업로드 URL 발급
curl -X POST http://localhost:8080/api/files/123/upload-url \
  -H "Authorization: Bearer YOUR_JWT_TOKEN"

# 응답 예시:
# {
#   "uploadUrl": "https://editus-dev-bucket.s3.ap-northeast-2.amazonaws.com/projects/1/files/123?X-Amz-Algorithm=...",
#   "contentKey": "projects/1/files/123"
# }
```

### 2. 프론트엔드에서 테스트

1. 브라우저 새로고침 (JWT 토큰 갱신)
2. 프로젝트 열기
3. 파일 생성
4. 내용 입력 후 Ctrl+S (또는 30초 자동 저장 대기)
5. 개발자 도구 → Network 탭에서 확인:
   - `POST /api/files/{fileId}/upload-url` - 성공
   - `PUT https://editus-dev-bucket.s3.amazonaws.com/...` - S3 업로드 성공
6. 파일 닫고 다시 열어서 내용 확인

---

## 🐛 트러블슈팅

### 에러 1: "Access Denied"

**원인**: AWS Credentials가 없거나 잘못됨

**해결**:
```bash
aws configure list  # 현재 설정 확인
aws s3 ls s3://editus-dev-bucket  # S3 접근 테스트
```

### 에러 2: "The specified bucket does not exist"

**원인**: S3 버킷이 생성되지 않았거나 이름이 다름

**해결**:
```bash
aws s3 ls  # 모든 버킷 목록 확인
aws s3 mb s3://editus-dev-bucket --region ap-northeast-2  # 버킷 생성
```

### 에러 3: "Presigned URL expired"

**원인**: Presigned URL은 3분간만 유효

**해결**:
- URL 발급 후 3분 이내에 업로드/다운로드 완료
- 시간 초과 시 새 URL 재발급

### 에러 4: "S3에서 파일 다운로드 실패"

**원인**: 파일이 아직 S3에 업로드되지 않음

**해결**:
- 새 파일은 처음 저장할 때까지 content가 비어있음
- 최소 1번 저장(Ctrl+S) 후 재조회

---

## 💰 AWS S3 비용

### 무료 티어 (신규 가입 12개월)
- 5GB 스토리지
- 20,000 GET 요청
- 2,000 PUT 요청

### 무료 티어 이후
- 스토리지: $0.023/GB/월
- PUT/POST 요청: $0.005/1,000 요청
- GET 요청: $0.0004/1,000 요청

**예상 비용** (소규모 프로젝트):
- 스토리지 1GB + 월 10만 요청 = 약 $0.05/월 (매우 저렴)

---

## 📝 체크리스트

**필수 작업**:
- [ ] AWS 계정 생성
- [ ] S3 버킷 생성 (`editus-dev-bucket`)
- [ ] IAM 사용자 생성 및 권한 부여
- [ ] Access Key/Secret Key 발급
- [ ] AWS CLI 설정 또는 환경 변수 설정
- [ ] 백엔드 재시작
- [ ] 브라우저 새로고침
- [ ] 파일 생성 및 저장 테스트

**선택 작업**:
- [ ] S3 버킷 수명 주기 정책 설정 (오래된 파일 자동 삭제)
- [ ] CloudWatch로 모니터링 설정
- [ ] 프로덕션 환경용 별도 버킷 생성

---

## 🎯 다음 단계

1. **로컬 개발**: AWS CLI로 credentials 설정 후 테스트
2. **도커 환경**: 환경 변수로 AWS credentials 전달
3. **프로덕션**: IAM Role 사용 (EC2/ECS에서 자동 인증)

---

## 📚 참고 자료

- [AWS S3 시작하기](https://docs.aws.amazon.com/ko_kr/AmazonS3/latest/userguide/GetStartedWithS3.html)
- [AWS CLI 설치](https://docs.aws.amazon.com/ko_kr/cli/latest/userguide/getting-started-install.html)
- [Spring Boot + S3 Integration](https://docs.awspring.io/spring-cloud-aws/docs/3.0.0/reference/html/index.html#amazon-s3)
- [S3 Presigned URL](https://docs.aws.amazon.com/AmazonS3/latest/userguide/PresignedUrlUploadObject.html)
