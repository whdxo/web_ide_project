# 🔧 Backend 실행 가이드

## 📋 백엔드 팀원 전용 가이드

이 문서는 **백엔드 개발자**(이세종, 방영진, 김민규, 양종태)를 위한 실행 가이드입니다.

---

## 1. 사전 준비 (백엔드 개발자 필수)

### ✅ 필수 설치 항목

#### Java 17
```bash
java -version
# 출력: openjdk version "17.0.x"
```
- 없으면: https://adoptium.net/

#### Docker Desktop
```bash
docker --version
docker ps
```
- **Mac**: [Docker Desktop for Mac](https://www.docker.com/products/docker-desktop/)
- **Windows**: [Docker Desktop for Windows](https://www.docker.com/products/docker-desktop/)
- **중요**: Docker Desktop을 실행해둔 상태에서 작업!

#### Git
```bash
git --version
```

---

## 2. 프로젝트 클론 및 환경 설정

### 2-1. 레포지토리 클론
```bash
git clone https://github.com/whdxo/web_ide_project.git
cd web_ide_project
```

### 2-2. 환경변수 파일 생성
```bash
# 프로젝트 루트에서
cp .env.example .env
```

`.env` 파일 확인 (팀에서 합의한 값):
```bash
DB_ROOT_PASSWORD=editus1234
DB_NAME=editus_dev
DB_USER=editus
DB_PASSWORD=editus1234
JWT_SECRET=editus-web-ide-jwt-secret-key-2024-change-this-in-production-environment
```

---

## 3. MySQL 실행 (Docker)

### 3-1. Docker Desktop 실행 확인
```bash
docker ps
```
- 에러 없이 실행되면 OK
- 에러 나면 Docker Desktop 켜기

### 3-2. MySQL 컨테이너 시작
```bash
# 프로젝트 루트에서
docker-compose up -d mysql
```

**⚠️ 포트 안내**: 로컬 MySQL과 충돌 방지를 위해 Docker MySQL은 **3307** 포트 사용

### 3-3. 컨테이너 확인
```bash
docker ps
```
출력 예시:
```
CONTAINER ID   IMAGE       PORTS                    NAMES
abc123def456   mysql:8.0   0.0.0.0:3307->3306/tcp   editus-mysql
```

### 3-4. MySQL 접속 테스트 (선택)
```bash
docker exec -it editus-mysql mysql -u editus -p
# 비밀번호: editus1234

mysql> show databases;
mysql> use editus_dev;
mysql> show tables;
mysql> exit;
```

---

## 4. 전체 서비스 실행 (터미널 3개 사용) ⭐

> **중요**: 백엔드 개발자도 API가 제대로 동작하는지 확인하려면 **MySQL + Backend + Frontend**를 모두 실행해야 합니다!

### 터미널 구성
- **터미널 1**: MySQL (Docker)
- **터미널 2**: Backend (Spring Boot)
- **터미널 3**: Frontend (Vite)

---

### 터미널 1: MySQL 실행 (Docker)

```bash
# 프로젝트 루트에서
cd /Users/jongtae/Desktop/web_ide

# MySQL 컨테이너 시작
docker-compose up mysql
```

**실행 확인:**
```
editus-mysql  | [System] [MY-010931] [Server] /usr/sbin/mysqld: ready for connections.
```

**포트**: 3307 (로컬 MySQL과 충돌 방지)

**⚠️ 참고**:
- 이 터미널은 **백그라운드로 두고** 그대로 놔두세요
- 중단하려면: `Ctrl + C`
- 백그라운드로 실행하려면: `docker-compose up -d mysql` (로그 안 보임)

---

### 터미널 2: Backend 실행 (Spring Boot)

**새 터미널을 열고:**

```bash
# 프로젝트 루트에서 backend 폴더로 이동
cd /Users/jongtae/Desktop/web_ide/backend

# Gradle로 Spring Boot 실행
./gradlew bootRun
```

**Windows 사용자:**
```bash
gradlew.bat bootRun
```

**실행 확인:**
```
Started EditUsApplication in 3.456 seconds (process running for 4.123)
Tomcat started on port 8080
Database version: 8.0.44
```

**포트**: 8080

**⚠️ 참고**:
- 이 터미널도 **그대로 놔두세요** (API 요청 로그 확인용)
- 중단하려면: `Ctrl + C`

---

### 터미널 3: Frontend 실행 (Vite)

**새 터미널을 열고:**

```bash
# 프로젝트 루트에서 frontend 폴더로 이동
cd /Users/jongtae/Desktop/web_ide/frontend

# Vite 개발 서버 실행
npm run dev
```

**실행 확인:**
```
VITE v6.0.0  ready in 523 ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

**포트**: 5173

**⚠️ 참고**:
- 이 터미널도 **그대로 놔두세요**
- 중단하려면: `Ctrl + C`

---

### 전체 동작 확인

#### 1. 브라우저에서 프론트엔드 접속
```
http://localhost:5173
```

#### 2. 개발자 도구 열기
- **Mac**: `Cmd + Option + I`
- **Windows**: `F12` 또는 `Ctrl + Shift + I`

#### 3. Network 탭에서 API 호출 확인
1. **Network** 탭 클릭
2. 페이지 새로고침 (`F5`)
3. 백엔드 API 요청 확인 (예: `/api/auth/me`, `/api/projects`)

#### 4. 터미널 2 (Backend)에서 로그 확인
```
2024-12-10 14:30:15.123  INFO ... : GET "/api/auth/me" - 200 OK
2024-12-10 14:30:16.456  INFO ... : GET "/api/projects" - 200 OK
```

#### 5. 정상 동작 확인
- [ ] 프론트엔드 화면이 보임 (http://localhost:5173)
- [ ] 백엔드 API 호출이 성공함 (Network 탭에서 200 응답)
- [ ] 터미널 2에서 API 로그가 출력됨

---

### 중단 방법

**역순으로 중단:**
1. **터미널 3** (Frontend): `Ctrl + C`
2. **터미널 2** (Backend): `Ctrl + C`
3. **터미널 1** (MySQL): `Ctrl + C`

**또는 MySQL만 백그라운드로 유지:**
```bash
# 터미널 1에서 Ctrl + C로 중단 후
docker-compose stop mysql

# 다음 실행 시
docker-compose start mysql  # 또는 docker-compose up -d mysql
```

---

### 💡 팀원들을 위한 팁

#### 매일 작업 시작할 때
```bash
# 터미널 1
docker-compose up -d mysql  # 백그라운드 실행

# 터미널 2
cd backend && ./gradlew bootRun

# 터미널 3
cd frontend && npm run dev
```

#### 작업 끝날 때
```bash
# 터미널 2, 3에서 Ctrl + C

# MySQL도 중단하려면
docker-compose stop mysql
```

#### 포트 충돌 해결
**8080 포트 충돌:**
```bash
# Mac/Linux
lsof -i :8080
kill -9 [PID]

# Windows
netstat -ano | findstr :8080
taskkill /PID [PID] /F
```

**3307 포트 충돌:**
```bash
docker-compose down
docker-compose up -d mysql
```

---

### 대안: 백엔드만 실행 (Postman 테스트용)

프론트엔드 없이 **백엔드만** 테스트하려면:

```bash
# 터미널 1: MySQL
docker-compose up -d mysql

# 터미널 2: Backend
cd backend && ./gradlew bootRun
```

**Postman으로 API 테스트:**
```
POST http://localhost:8080/api/auth/login
Content-Type: application/json

{
  "email": "test@example.com",
  "password": "1234"
}
```

**또는 curl로 테스트:**
```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"1234"}'
```

---

## 5. 프로젝트 구조 (Backend)

```
backend/
├── src/main/java/com/editus/backend/
│   ├── domain/               # 도메인별 비즈니스 로직
│   │   ├── auth/            # 👤 이세종 담당 (BE 파트 A)
│   │   │   ├── controller/  # AuthController, UserController
│   │   │   ├── service/     # AuthService, UserService
│   │   │   ├── repository/  # UserRepository
│   │   │   ├── dto/         # LoginRequest, SignupRequest, UserResponse
│   │   │   └── entity/      # User
│   │   │
│   │   ├── project/         # 📁 방영진 담당 (BE 파트 B)
│   │   │   ├── controller/  # ProjectController, MemberController
│   │   │   ├── service/     # ProjectService, MemberService
│   │   │   ├── repository/  # ProjectRepository, MemberRepository
│   │   │   ├── dto/         # ProjectResponse, MemberRequest
│   │   │   └── entity/      # Project, ProjectMember
│   │   │
│   │   ├── chat/            # 💬 방영진 담당 (BE 파트 B)
│   │   │   ├── controller/  # ChatController (WebSocket)
│   │   │   ├── service/     # ChatService
│   │   │   ├── repository/  # ChatMessageRepository, ChatRoomRepository
│   │   │   ├── dto/         # ChatMessageRequest, ChatMessageResponse
│   │   │   └── entity/      # ChatMessage, ChatRoom
│   │   │
│   │   ├── file/            # 📄 김민규 담당 (BE 파트 C)
│   │   │   ├── controller/  # FileController, FolderController
│   │   │   ├── service/     # FileService, FolderService, FileTreeService
│   │   │   ├── repository/  # FileRepository, FolderRepository
│   │   │   ├── dto/         # FileRequest, FileResponse, FileTreeResponse
│   │   │   └── entity/      # File, Folder
│   │   │
│   │   ├── schedule/        # 📅 양종태 담당 (BE 파트 D)
│   │   │   ├── controller/  # SprintController, TodoController, TaskController
│   │   │   ├── service/     # SprintService, TodoService, TaskService
│   │   │   ├── repository/  # SprintRepository, TodoRepository, TaskRepository
│   │   │   ├── dto/         # TodoRequest, TaskResponse
│   │   │   └── entity/      # Sprint, Todo, Task
│   │   │
│   │   └── ai/              # 🤖 양종태 담당 (BE 파트 D)
│   │       ├── controller/  # AIReviewController
│   │       ├── service/     # AIReviewService
│   │       └── dto/         # AIReviewRequest, AIReviewResponse
│   │
│   ├── global/              # ⚠️ 공통 설정 (팀장 승인 필요)
│   │   ├── config/          # SecurityConfig (이세종), WebSocketConfig (방영진), JpaConfig
│   │   ├── security/        # JWT 관련 (이세종): JwtTokenProvider, JwtAuthenticationFilter
│   │   ├── common/          # 공통 DTO: ApiResponse, ErrorResponse, PageResponse
│   │   └── audit/           # BaseTimeEntity (createdAt, updatedAt)
│   │
│   └── EditUsApplication.java  # ⚠️ 수정 금지!
│
├── src/main/resources/
│   ├── application.yml           # ⚠️ 수정 금지!
│   ├── application-local.yml     # 로컬 설정
│   ├── application-dev.yml       # 개발 서버 설정
│   └── db/migration/             # Flyway 마이그레이션
│
├── build.gradle                  # ⚠️ 의존성 추가 시 팀장 승인
└── Dockerfile
```

---

## 6. 팀원별 담당 영역 및 작업 가이드

### 👤 이세종 - 인증/회원 관리/Security (`domain/auth/`) - BE 파트 A

**담당 기능:**
- 로그인/로그아웃/회원가입
- 비밀번호 변경
- 내 정보 조회
- **Spring Security 설정**
- **JWT 발급/검증**
- **인증/인가 필터 설계**

**작업 영역:**
```
backend/src/main/java/com/editus/backend/
├── domain/auth/
│   ├── controller/
│   │   ├── AuthController.java     # 로그인/로그아웃/토큰 갱신
│   │   └── UserController.java     # 회원가입/내 정보 조회
│   ├── service/
│   │   ├── AuthService.java
│   │   └── UserService.java
│   ├── repository/
│   │   └── UserRepository.java
│   ├── dto/
│   │   ├── LoginRequest.java
│   │   ├── LoginResponse.java
│   │   ├── SignupRequest.java
│   │   └── UserResponse.java
│   └── entity/
│       └── User.java
│
└── global/
    ├── config/
    │   └── SecurityConfig.java     # ⭐ 이세종 담당
    └── security/jwt/
        ├── JwtTokenProvider.java   # ⭐ 이세종 담당
        └── JwtAuthenticationFilter.java
```

**API 엔드포인트:**
- `POST /api/auth/login` - 로그인 (JWT 발급)
- `POST /api/auth/logout` - 로그아웃
- `POST /api/auth/refresh` - 토큰 갱신
- `PATCH /api/auth/password` - 비밀번호 변경
- `POST /api/users` - 회원가입
- `GET /api/users/me` - 내 정보 조회

**사용할 global 설정 (본인이 작성):**
- `global/config/SecurityConfig.java` - Spring Security 설정
- `global/security/jwt/JwtTokenProvider.java` - JWT 토큰 생성/검증
- `global/security/jwt/JwtAuthenticationFilter.java` - JWT 인증 필터
- `global/common/dto/ApiResponse.java` - 표준 응답 형식
- `global/audit/BaseTimeEntity.java` - 생성일/수정일 자동 관리

**협업 대상:**
- **FE 파트 1 (박영선)**: 로그인/회원가입 UI, 토큰 관리
- **BE 파트 D (양종태)**: Todo/Task/AI API 인증 흐름 공유
- **전체 BE**: Security 설정은 전체 API에 영향

**코드 예시:**
```java
// AuthController.java
@RestController
@RequestMapping("/api/auth")
public class AuthController {

    @PostMapping("/login")
    public ResponseEntity<ApiResponse<LoginResponse>> login(@RequestBody LoginRequest request) {
        LoginResponse response = authService.login(request);
        return ResponseEntity.ok(ApiResponse.success("로그인 성공", response));
    }
}

// SecurityConfig.java (본인 담당)
@Configuration
@EnableWebSecurity
public class SecurityConfig {

    @Bean
    public SecurityFilterChain filterChain(HttpSecurity http) throws Exception {
        http
            .csrf(csrf -> csrf.disable())
            .authorizeHttpRequests(auth -> auth
                .requestMatchers("/api/auth/**", "/api/users").permitAll()
                .anyRequest().authenticated()
            )
            .addFilterBefore(jwtAuthenticationFilter, UsernamePasswordAuthenticationFilter.class);
        return http.build();
    }
}
```

---

### 📁 방영진 - 프로젝트 멤버 관리 + 실시간 채팅 (`domain/project/`, `domain/chat/`) - BE 파트 B

**담당 기능:**
- 프로젝트 멤버 조회/추가/삭제
- 프로젝트 내 역할(Role) 관리
- **WebSocket/STOMP 환경 구축**
- **채팅 메시지 송수신 구조 개발**

**작업 영역:**
```
backend/src/main/java/com/editus/backend/
├── domain/project/
│   ├── controller/
│   │   ├── ProjectController.java
│   │   └── MemberController.java
│   ├── service/
│   │   ├── ProjectService.java
│   │   └── MemberService.java
│   ├── repository/
│   │   ├── ProjectRepository.java
│   │   └── MemberRepository.java
│   ├── dto/
│   │   ├── ProjectResponse.java
│   │   └── MemberRequest.java
│   └── entity/
│       ├── Project.java
│       └── ProjectMember.java
│
├── domain/chat/
│   ├── controller/
│   │   └── ChatController.java     # WebSocket STOMP
│   ├── service/
│   │   └── ChatService.java
│   ├── repository/
│   │   ├── ChatMessageRepository.java
│   │   └── ChatRoomRepository.java
│   ├── dto/
│   │   ├── ChatMessageRequest.java
│   │   └── ChatMessageResponse.java
│   └── entity/
│       ├── ChatMessage.java
│       └── ChatRoom.java
│
└── global/config/
    └── WebSocketConfig.java        # ⭐ 방영진 담당
```

**API 엔드포인트:**

**Project & Member API:**
- `GET /api/projects` - 프로젝트 목록 조회
- `GET /api/projects/{projectId}/members` - 프로젝트 멤버 조회
- `POST /api/projects/{projectId}/members` - 멤버 추가
- `DELETE /api/projects/members/{memberId}` - 멤버 삭제

**Chat API (WebSocket):**
- WebSocket 연결: `ws://localhost:8080/ws`
- STOMP 구독: `/topic/project.{projectId}`
- STOMP 전송: `/app/chat.send`
- REST API: `GET /api/chat/messages?projectId={id}` - 메시지 이력 조회

**사용할 global 설정:**
- `global/config/WebSocketConfig.java` - **본인이 작성** (WebSocket 설정)
- `global/common/dto/ApiResponse.java`
- `global/audit/BaseTimeEntity.java`
- `global/security/jwt/JwtTokenProvider.java` - 이세종이 작성 (WebSocket 인증 시 사용)

**협업 대상:**
- **FE 파트 2 (박유경)**: IDE 채팅 패널 UI 및 실시간 메시지 연동
- **BE 파트 A (이세종)**: 인증 사용자만 채팅·멤버 권한 가능
- **BE 파트 C (김민규)**: 프로젝트 구조 ID 공유

**코드 예시:**
```java
// WebSocketConfig.java (본인 담당)
@Configuration
@EnableWebSocketMessageBroker
public class WebSocketConfig implements WebSocketMessageBrokerConfigurer {

    @Override
    public void configureMessageBroker(MessageBrokerRegistry config) {
        config.enableSimpleBroker("/topic");
        config.setApplicationDestinationPrefixes("/app");
    }

    @Override
    public void registerStompEndpoints(StompEndpointRegistry registry) {
        registry.addEndpoint("/ws")
                .setAllowedOriginPatterns("*")
                .withSockJS();
    }
}

// ChatController.java
@Controller
public class ChatController {

    @MessageMapping("/chat.send")
    @SendTo("/topic/project.{projectId}")
    public ChatMessageResponse sendMessage(@Payload ChatMessageRequest message) {
        return chatService.saveAndBroadcast(message);
    }
}
```

---

### 📄 김민규 - 파일/폴더 트리 + 파일 CRUD (`domain/file/`) - BE 파트 C

**담당 기능:**
- 프로젝트 파일 트리 구조 조회
- 폴더 생성/삭제
- 파일 생성/삭제
- 파일 내용 조회/저장
- **DB 기반 계층형 트리 구조 설계**

**작업 영역:**
```
backend/src/main/java/com/editus/backend/domain/file/
├── controller/
│   ├── FileController.java
│   └── FolderController.java
├── service/
│   ├── FileService.java
│   ├── FolderService.java
│   └── FileTreeService.java       # 트리 구조 변환
├── repository/
│   ├── FileRepository.java
│   └── FolderRepository.java
├── dto/
│   ├── FileRequest.java
│   ├── FileResponse.java
│   ├── FileContentRequest.java
│   └── FileTreeResponse.java      # 계층 구조 DTO
└── entity/
    ├── File.java
    └── Folder.java
```

**API 엔드포인트:**
- `GET /api/projects/{projectId}/tree` - 파일 트리 전체 조회
- `POST /api/projects/{projectId}/folders` - 폴더 생성
- `POST /api/projects/{projectId}/files` - 파일 생성
- `GET /api/files/{fileId}` - 파일 메타데이터 조회
- `PUT /api/files/{fileId}` - 파일 내용 저장
- `DELETE /api/files/{fileId}` - 파일 삭제
- `DELETE /api/folders/{folderId}` - 폴더 삭제

**사용할 global 설정:**
- `global/common/dto/ApiResponse.java`
- `global/audit/BaseTimeEntity.java`

**협업 대상:**
- **FE 파트 2 (박유경)**: 파일 트리 UI 및 에디터 연동
- **BE 파트 D (양종태)**: AI 리뷰 요청 시 fileId/fileContent 제공
- **BE 파트 A (이세종)**: 프로젝트 접근 권한 확인

**코드 예시:**
```java
// FileTreeService.java
@Service
public class FileTreeService {

    public FileTreeResponse getProjectTree(Long projectId) {
        List<Folder> folders = folderRepository.findByProjectId(projectId);
        List<File> files = fileRepository.findByProjectId(projectId);

        // 계층 구조로 변환
        return buildTree(folders, files);
    }

    private FileTreeResponse buildTree(List<Folder> folders, List<File> files) {
        // 폴더-파일 트리 구조 생성 로직
    }
}

// FileController.java
@RestController
@RequestMapping("/api")
public class FileController {

    @GetMapping("/projects/{projectId}/tree")
    public ResponseEntity<ApiResponse<FileTreeResponse>> getFileTree(@PathVariable Long projectId) {
        FileTreeResponse tree = fileTreeService.getProjectTree(projectId);
        return ResponseEntity.ok(ApiResponse.success("파일 트리 조회 성공", tree));
    }

    @PutMapping("/files/{fileId}")
    public ResponseEntity<ApiResponse<Void>> saveFileContent(
            @PathVariable Long fileId,
            @RequestBody FileContentRequest request) {
        fileService.saveContent(fileId, request.getContent());
        return ResponseEntity.ok(ApiResponse.success("파일 저장 성공", null));
    }
}
```

---

### 📅 양종태 - 일정 관리 + AI 코드 리뷰 (`domain/schedule/`, `domain/ai/`) - BE 파트 D

**담당 기능:**
- 스프린트 생성/조회
- 개인 Todo CRUD
- 팀 Task(칸반) 생성/상태 변경/담당자 연동
- **AI 코드 리뷰** (파일 내용 기반 분석)

**작업 영역:**
```
backend/src/main/java/com/editus/backend/
├── domain/schedule/
│   ├── controller/
│   │   ├── SprintController.java
│   │   ├── TodoController.java
│   │   └── TaskController.java
│   ├── service/
│   │   ├── SprintService.java
│   │   ├── TodoService.java
│   │   └── TaskService.java
│   ├── repository/
│   │   ├── SprintRepository.java
│   │   ├── TodoRepository.java
│   │   └── TaskRepository.java
│   ├── dto/
│   │   ├── TodoRequest.java
│   │   ├── TaskRequest.java
│   │   └── TaskResponse.java
│   └── entity/
│       ├── Sprint.java
│       ├── Todo.java
│       └── Task.java
│
└── domain/ai/
    ├── controller/
    │   └── AIReviewController.java
    ├── service/
    │   └── AIReviewService.java
    └── dto/
        ├── AIReviewRequest.java
        └── AIReviewResponse.java
```

**API 엔드포인트:**

**Schedule API:**
- `GET /api/projects/{projectId}/sprints` - 스프린트 목록
- `POST /api/projects/{projectId}/sprints` - 스프린트 생성
- `GET /api/todos` - 개인 Todo 목록
- `POST /api/todos` - Todo 생성
- `GET /api/projects/{projectId}/tasks` - 팀 Task 목록
- `POST /api/projects/{projectId}/tasks` - Task 생성
- `PATCH /api/tasks/{taskId}/status` - Task 상태 변경

**AI Review API:**
- `POST /api/ai/review` - AI 코드 리뷰 요청
  - Request: `{ fileId, fileContent }`
  - Response: `{ suggestions, issues, score }`

**사용할 global 설정:**
- `global/common/dto/ApiResponse.java`
- `global/audit/BaseTimeEntity.java`

**협업 대상:**
- **FE 파트 2 (박유경)**: Todo/Task/AI 패널 연동
- **BE 파트 C (김민규)**: AI 리뷰 시 파일 내용 조회
- **BE 파트 A (이세종)**: 사용자/프로젝트 권한 체크

**코드 예시:**
```java
// TaskController.java
@RestController
@RequestMapping("/api")
public class TaskController {

    @GetMapping("/projects/{projectId}/tasks")
    public ResponseEntity<ApiResponse<List<TaskResponse>>> getTasks(@PathVariable Long projectId) {
        List<TaskResponse> tasks = taskService.getProjectTasks(projectId);
        return ResponseEntity.ok(ApiResponse.success("Task 목록 조회 성공", tasks));
    }

    @PatchMapping("/tasks/{taskId}/status")
    public ResponseEntity<ApiResponse<Void>> updateTaskStatus(
            @PathVariable Long taskId,
            @RequestParam String status) {
        taskService.updateStatus(taskId, status);
        return ResponseEntity.ok(ApiResponse.success("Task 상태 변경 성공", null));
    }
}

// AIReviewController.java
@RestController
@RequestMapping("/api/ai")
public class AIReviewController {

    @PostMapping("/review")
    public ResponseEntity<ApiResponse<AIReviewResponse>> reviewCode(
            @RequestBody AIReviewRequest request) {
        AIReviewResponse review = aiReviewService.analyzeCode(request);
        return ResponseEntity.ok(ApiResponse.success("AI 리뷰 완료", review));
    }
}
```

---

## 7. ⚠️ 주의사항 (반드시 읽기!)

### 🚫 절대 수정하면 안 되는 파일

1. **`EditUsApplication.java`** - 메인 애플리케이션 클래스
2. **`application.yml`** - 기본 설정 파일
3. **`build.gradle`** - 의존성 추가 시 팀장에게 먼저 확인
4. **`.env` 파일** - Git에 올리지 말 것! (`.gitignore`에 포함됨)

### ⚠️ 팀장 승인 필요한 변경사항

1. **`global/` 패키지 수정 (단, 본인 담당 제외)**
   - **이세종**: `SecurityConfig`, `JwtTokenProvider` 자유 수정 가능
   - **방영진**: `WebSocketConfig` 자유 수정 가능
   - **그 외 global 공통 파일**: 팀장 승인 필요

2. **의존성 추가 (`build.gradle`)**
   - 새로운 라이브러리 추가 시 팀장 승인 필요
   - 예: Spring Security, WebSocket, Redis 등

3. **데이터베이스 스키마 변경**
   - `entity/` 클래스 변경 시 다른 도메인에 영향 확인
   - Flyway 마이그레이션 스크립트 작성 필요

### ✅ 자유롭게 작업 가능한 영역

1. **자신의 `domain/` 디렉토리 내부**
   - `domain/auth/` (이세종)
   - `domain/project/`, `domain/chat/` (방영진)
   - `domain/file/` (김민규)
   - `domain/schedule/`, `domain/ai/` (양종태)

2. **자신이 담당한 `global/` 설정**
   - `SecurityConfig`, `JwtTokenProvider` (이세종)
   - `WebSocketConfig` (방영진)

3. **자신의 도메인 테스트 코드**
   - `src/test/java/com/editus/backend/domain/auth/` 등

### 📝 코딩 컨벤션

#### 패키지 구조
```java
com.editus.backend.domain.{도메인명}.controller
com.editus.backend.domain.{도메인명}.service
com.editus.backend.domain.{도메인명}.repository
com.editus.backend.domain.{도메인명}.dto
com.editus.backend.domain.{도메인명}.entity
```

#### 클래스 네이밍
- **Controller**: `{도메인명}Controller.java` (예: `AuthController.java`)
- **Service**: `{도메인명}Service.java` (예: `AuthService.java`)
- **Repository**: `{엔티티명}Repository.java` (예: `UserRepository.java`)
- **Entity**: `{엔티티명}.java` (예: `User.java`)
- **DTO**: `{목적}{Request|Response}.java` (예: `LoginRequest.java`)

#### API 응답 형식 (공통)
```java
@GetMapping("/api/auth/me")
public ResponseEntity<ApiResponse<UserResponse>> getCurrentUser() {
    UserResponse user = authService.getCurrentUser();
    return ResponseEntity.ok(ApiResponse.success("사용자 정보 조회 성공", user));
}
```

#### 예외 처리 (공통)
```java
// 비즈니스 예외는 RuntimeException 상속
public class UserNotFoundException extends RuntimeException {
    public UserNotFoundException(String message) {
        super(message);
    }
}

// GlobalExceptionHandler가 자동으로 처리
```

---

## 8. Git 브랜치 전략

### 브랜치 이름 규칙
```
feature/{도메인명}-{기능명}

예시:
- feature/auth-login (이세종)
- feature/chat-websocket (방영진)
- feature/file-tree-api (김민규)
- feature/schedule-crud (양종태)
```

### 작업 순서
```bash
# 1. develop 브랜치에서 최신 코드 받기
git checkout develop
git pull origin develop

# 2. 기능 브랜치 생성
git checkout -b feature/auth-login

# 3. 작업 후 커밋
git add .
git commit -m "feat(auth): 로그인 API 구현"

# 4. Push
git push origin feature/auth-login

# 5. GitHub에서 Pull Request 생성
# develop <- feature/auth-login
```

### 커밋 메시지 컨벤션
```
feat(도메인명): 기능 설명
fix(도메인명): 버그 수정 설명
docs(도메인명): 문서 수정
test(도메인명): 테스트 추가/수정

예시:
feat(auth): JWT 토큰 발급 기능 구현
fix(file): 파일 저장 시 인코딩 오류 수정
docs(api): Auth API 문서 추가
test(chat): 채팅 메시지 전송 테스트 추가
```

---

## 9. 문제 해결

### 🔴 문제 1: MySQL 연결 실패
**에러:**
```
Cannot connect to MySQL
Connection refused
```

**해결:**
1. Docker Desktop 실행 확인
2. MySQL 컨테이너 확인: `docker ps`
3. 컨테이너 재시작: `docker-compose restart mysql`
4. 로그 확인: `docker logs editus-mysql`

---

### 🔴 문제 2: 포트 8080 충돌
**에러:**
```
Port 8080 is already in use
```

**해결 (Mac/Linux):**
```bash
lsof -i :8080
kill -9 [PID]
```

**해결 (Windows):**
```bash
netstat -ano | findstr :8080
taskkill /PID [PID] /F
```

---

### 🔴 문제 3: Gradle 빌드 실패
**에러:**
```
Could not resolve dependencies
```

**해결:**
```bash
cd backend
./gradlew clean build --refresh-dependencies
```

---

### 🔴 문제 4: JWT 시크릿 관련 에러
**에러:**
```
JWT secret key is not configured
```

**해결:**
1. 프로젝트 루트의 `.env` 파일 확인
2. `JWT_SECRET` 값이 있는지 확인
3. 백엔드 재시작

---

## 10. 빠른 시작 체크리스트

- [ ] Docker Desktop 실행
- [ ] `.env` 파일 생성 및 확인
- [ ] `docker-compose up -d mysql`
- [ ] `docker ps`로 MySQL 컨테이너 확인
- [ ] `cd backend`
- [ ] `./gradlew bootRun`
- [ ] 브라우저에서 `http://localhost:8080` 접속
- [ ] 본인 담당 도메인 폴더에서 작업 시작

---

## 📞 문제 발생 시

1. 이 가이드의 [문제 해결](#9-문제-해결) 섹션 확인
2. 팀 채팅방에 에러 메시지 공유
3. `docker logs editus-mysql` 출력 공유
4. 백엔드 콘솔 로그 캡처해서 공유

---

**작성일**: 2024-12-10
**작성자**: EditUs Backend Team
**버전**: 2.0 (역할 분담 수정)
