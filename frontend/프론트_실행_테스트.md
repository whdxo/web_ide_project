# 🎨 Frontend 실행 가이드

## 📋 프론트엔드 팀원 전용 가이드

이 문서는 **프론트엔드 개발자**(박영선, 박유경)를 위한 실행 가이드입니다.

---

## 1. 사전 준비 (프론트엔드 개발자 필수)

### ✅ 필수 설치 항목

#### Node.js 18+ (LTS)
```bash
node -v
npm -v
# 출력: v18.x.x 또는 v20.x.x
```
- 없으면: https://nodejs.org/ 에서 LTS 버전 다운로드

#### Git
```bash
git --version
```

### ❌ 필요 없는 것들

- **Docker Desktop** - 불필요! (백엔드 팀원만 필요)
- **Java** - 불필요! (백엔드 팀원만 필요)
- **MySQL** - 불필요! (백엔드가 실행 중이면 됨)

---

## 2. 프로젝트 클론

### 2-1. 레포지토리 클론
```bash
git clone https://github.com/whdxo/wed_ide_project.git
cd wed_ide_project
```

### 2-2. 프론트엔드 디렉토리로 이동
```bash
cd frontend
```

---

## 3. 의존성 설치 (처음 한 번만)

```bash
npm install
```

설치되는 주요 패키지:
- **React 18** - UI 라이브러리
- **TypeScript** - 타입 안정성
- **Vite** - 빌드 툴
- **Zustand** - 상태 관리
- **Axios** - HTTP 클라이언트
- **Monaco Editor** - 코드 에디터
- **Tailwind CSS** - 스타일링
- **STOMP/SockJS** - WebSocket (실시간 채팅)

---

## 4. 프론트엔드 실행

### 4-1. 개발 서버 실행
```bash
npm run dev
```

### 4-2. 실행 확인

터미널에 다음 메시지가 나옵니다:
```
VITE v5.x.x  ready in XXX ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

### 4-3. 브라우저 테스트
```
http://localhost:5173
```
- React 기본 페이지 또는 로그인 페이지가 나오면 성공!

### 4-4. 중지
터미널에서 `Ctrl + C`

---

## 5. 프로젝트 구조 (Frontend)

```
frontend/
├── src/
│   ├── features/                # 기능별 모듈
│   │   ├── auth/               # 🔐 박영선 담당 (FE 파트 1)
│   │   │   ├── components/     # LoginForm, SignupForm, KakaoLoginButton
│   │   │   ├── hooks/          # useAuth, useLogin, useSignup
│   │   │   ├── api/            # authApi.ts
│   │   │   ├── types/          # auth.types.ts
│   │   │   └── store/          # authStore.ts
│   │   │
│   │   ├── project/            # 📁 박영선 담당 (FE 파트 1)
│   │   │   ├── components/     # ProjectList, ProjectCard, ProjectSelector
│   │   │   ├── hooks/          # useProjects, useProjectSelection
│   │   │   ├── api/            # projectApi.ts
│   │   │   ├── types/          # project.types.ts
│   │   │   └── store/          # projectStore.ts
│   │   │
│   │   ├── editor/             # ✏️ 박유경 담당 (FE 파트 2)
│   │   │   ├── components/     # MonacoEditor, EditorTabs, EditorToolbar
│   │   │   ├── hooks/          # useEditor, useAutoSave, useFileContent
│   │   │   ├── api/            # editorApi.ts
│   │   │   ├── types/          # editor.types.ts
│   │   │   └── store/          # editorStore.ts
│   │   │
│   │   ├── fileTree/           # 🌲 박유경 담당 (FE 파트 2)
│   │   │   ├── components/     # FileTree, FileNode, ContextMenu, CreateModal
│   │   │   ├── hooks/          # useFileTree, useFileOperations
│   │   │   ├── api/            # fileApi.ts
│   │   │   ├── types/          # file.types.ts
│   │   │   └── store/          # fileTreeStore.ts
│   │   │
│   │   ├── chat/               # 💬 박유경 담당 (FE 파트 2)
│   │   │   ├── components/     # ChatPanel, MessageList, MessageInput, UserList
│   │   │   ├── hooks/          # useChat, useWebSocket
│   │   │   ├── api/            # chatApi.ts
│   │   │   ├── types/          # chat.types.ts
│   │   │   └── store/          # chatStore.ts
│   │   │
│   │   ├── schedule/           # 📅 박유경 담당 (FE 파트 2)
│   │   │   ├── components/     # TodoList, TaskBoard, SprintView
│   │   │   ├── hooks/          # useTodo, useTask, useSprint
│   │   │   ├── api/            # scheduleApi.ts
│   │   │   ├── types/          # schedule.types.ts
│   │   │   └── store/          # scheduleStore.ts
│   │   │
│   │   └── ai/                 # 🤖 박유경 담당 (FE 파트 2)
│   │       ├── components/     # AIReviewPanel, ReviewResult
│   │       ├── hooks/          # useAIReview
│   │       ├── api/            # aiApi.ts
│   │       ├── types/          # ai.types.ts
│   │       └── store/          # aiStore.ts
│   │
│   ├── shared/                 # 🔧 박영선 담당 (FE 파트 1) - 공통 부분
│   │   ├── components/         # Button, Input, Modal, Loading
│   │   │   └── Layout/         # MainLayout, Header, Sidebar
│   │   ├── hooks/              # useDebounce, useLocalStorage, useTheme
│   │   ├── utils/              # api.ts (Axios 인스턴스), constants.ts
│   │   └── types/              # common.types.ts, api.types.ts
│   │
│   ├── styles/
│   │   ├── index.css           # Tailwind imports
│   │   └── globals.css         # 전역 스타일
│   │
│   └── config/
│       ├── env.ts              # 환경 변수
│       └── routes.ts           # 라우트 정의
│
├── public/                     # 정적 파일
├── .env.development            # 개발 환경 변수
├── .env.production             # 프로덕션 환경 변수
├── package.json                # ⚠️ 의존성 추가 시 팀장 승인
├── vite.config.ts              # ⚠️ 수정 금지!
├── tsconfig.json               # TypeScript 설정
├── tailwind.config.ts          # Tailwind 설정
└── postcss.config.cjs          # PostCSS 설정
```

---

## 6. 팀원별 담당 영역 및 작업 가이드

### 🔐 박영선 - 인증 + 프로젝트 선택 + 전체 라우팅 + 공통 레이아웃 - FE 파트 1

**담당 기능:**
- 로그인/회원가입 UI
- 카카오 간편 로그인 버튼
- 프로젝트 목록/선택 페이지
- 전체 라우팅 구조 (`/login`, `/projects`, `/editor/:projectId`)
- **공통 레이아웃**(헤더, 공통 컴포넌트)

**작업 영역:**
```
frontend/src/
├── features/
│   ├── auth/
│   │   ├── components/
│   │   │   ├── LoginForm.tsx          # 로그인 폼
│   │   │   ├── SignupForm.tsx         # 회원가입 폼
│   │   │   ├── KakaoLoginButton.tsx   # 카카오 로그인
│   │   │   └── AuthLayout.tsx         # 인증 레이아웃
│   │   ├── hooks/
│   │   │   ├── useAuth.ts             # 인증 훅
│   │   │   ├── useLogin.ts            # 로그인 훅
│   │   │   └── useSignup.ts           # 회원가입 훅
│   │   ├── api/
│   │   │   └── authApi.ts             # 인증 API 호출
│   │   ├── types/
│   │   │   └── auth.types.ts          # 인증 관련 타입
│   │   └── store/
│   │       └── authStore.ts           # 인증 상태 관리 (Zustand)
│   │
│   └── project/
│       ├── components/
│       │   ├── ProjectList.tsx        # 프로젝트 목록
│       │   ├── ProjectCard.tsx        # 프로젝트 카드
│       │   └── ProjectSelector.tsx    # 프로젝트 선택
│       ├── hooks/
│       │   ├── useProjects.ts
│       │   └── useProjectSelection.ts
│       ├── api/
│       │   └── projectApi.ts
│       ├── types/
│       │   └── project.types.ts
│       └── store/
│           └── projectStore.ts
│
└── shared/                             # ⭐ 박영선 담당
    ├── components/
    │   ├── Button.tsx                  # 공통 버튼
    │   ├── Input.tsx                   # 공통 입력
    │   ├── Modal.tsx                   # 공통 모달
    │   ├── Loading.tsx                 # 로딩 스피너
    │   └── Layout/
    │       ├── MainLayout.tsx          # 메인 레이아웃
    │       ├── Header.tsx              # 헤더
    │       └── Sidebar.tsx             # 사이드바
    ├── hooks/
    │   ├── useDebounce.ts
    │   ├── useLocalStorage.ts
    │   └── useTheme.ts
    ├── utils/
    │   ├── api.ts                      # ⭐ Axios 인스턴스 설정
    │   └── constants.ts
    └── types/
        ├── common.types.ts
        └── api.types.ts
```

**주요 화면:**
1. **로그인 페이지** (`/login`)
   - 이메일/비밀번호 입력
   - "로그인" 버튼
   - "회원가입" 링크
   - 카카오 로그인 버튼

2. **회원가입 페이지** (`/signup`)
   - 이메일, 비밀번호, 이름 입력
   - "회원가입" 버튼

3. **프로젝트 목록 페이지** (`/projects`)
   - 프로젝트 카드 그리드
   - "새 프로젝트" 버튼
   - 프로젝트 선택 → IDE 페이지로 이동

**사용할 백엔드 API:**
- `POST /api/auth/login` - 로그인 (BE: 이세종)
- `POST /api/users` - 회원가입 (BE: 이세종)
- `GET /api/users/me` - 현재 사용자 정보 (BE: 이세종)
- `POST /api/auth/logout` - 로그아웃 (BE: 이세종)
- `GET /api/projects` - 프로젝트 목록 (BE: 방영진)

**협업 대상:**
- **BE 파트 A (이세종)**: 로그인/회원가입/토큰 플로우 연동
- **BE 파트 B (방영진)**: 프로젝트 목록/멤버 정보 요청
- **FE 파트 2 (박유경)**: 선택된 프로젝트 정보를 IDE 화면으로 넘김

**코드 예시:**
```typescript
// features/auth/api/authApi.ts
import { apiClient } from '@/shared/utils/api';
import type { LoginRequest, LoginResponse } from '../types/auth.types';

export const authApi = {
  login: async (data: LoginRequest): Promise<LoginResponse> => {
    const response = await apiClient.post('/api/auth/login', data);
    return response.data;
  },

  signup: async (data: SignupRequest) => {
    const response = await apiClient.post('/api/users', data);
    return response.data;
  },
};

// features/auth/store/authStore.ts
import { create } from 'zustand';

interface AuthState {
  user: User | null;
  token: string | null;
  isAuthenticated: boolean;
  login: (email: string, password: string) => Promise<void>;
  logout: () => void;
}

export const useAuthStore = create<AuthState>((set) => ({
  user: null,
  token: localStorage.getItem('token'),
  isAuthenticated: false,

  login: async (email, password) => {
    const response = await authApi.login({ email, password });
    localStorage.setItem('token', response.token);
    set({ user: response.user, token: response.token, isAuthenticated: true });
  },

  logout: () => {
    localStorage.removeItem('token');
    set({ user: null, token: null, isAuthenticated: false });
  },
}));

// shared/utils/api.ts (⭐ 박영선 담당)
import axios from 'axios';

export const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  headers: {
    'Content-Type': 'application/json',
  },
});

// 요청 인터셉터: JWT 토큰 자동 추가
apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 응답 인터셉터: 에러 처리
apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token');
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);
```

---

### ✏️ 박유경 - IDE 전체 UI + 에디터 + 파일트리 + 사이드 패널 4종 - FE 파트 2

**담당 기능:**
- **전체 IDE 레이아웃**
- **Monaco Editor 연결**
- **파일 트리 UI**
- **파일 탭 UI**
- **하단 터미널(기본) UI**
- **우측 사이드 패널 UI** (채팅, Todo/Task, AI 코드 리뷰, 설정)

**작업 영역:**
```
frontend/src/features/
├── editor/
│   ├── components/
│   │   ├── MonacoEditor.tsx          # Monaco 에디터
│   │   ├── EditorTabs.tsx            # 열린 파일 탭
│   │   └── EditorToolbar.tsx         # 에디터 툴바
│   ├── hooks/
│   │   ├── useEditor.ts              # 에디터 상태 관리
│   │   ├── useAutoSave.ts            # 자동 저장
│   │   └── useFileContent.ts         # 파일 내용 로드
│   ├── api/
│   │   └── editorApi.ts              # 파일 저장 API
│   ├── types/
│   │   └── editor.types.ts
│   └── store/
│       └── editorStore.ts
│
├── fileTree/
│   ├── components/
│   │   ├── FileTree.tsx              # 파일 트리
│   │   ├── FileNode.tsx              # 트리 노드
│   │   ├── ContextMenu.tsx           # 우클릭 메뉴
│   │   └── CreateModal.tsx           # 파일/폴더 생성 모달
│   ├── hooks/
│   │   ├── useFileTree.ts
│   │   └── useFileOperations.ts
│   ├── api/
│   │   └── fileApi.ts
│   ├── types/
│   │   └── file.types.ts
│   └── store/
│       └── fileTreeStore.ts
│
├── chat/
│   ├── components/
│   │   ├── ChatPanel.tsx             # 채팅 패널
│   │   ├── MessageList.tsx           # 메시지 목록
│   │   ├── MessageInput.tsx          # 메시지 입력
│   │   └── UserList.tsx              # 사용자 목록
│   ├── hooks/
│   │   ├── useChat.ts
│   │   └── useWebSocket.ts
│   ├── api/
│   │   └── chatApi.ts
│   ├── types/
│   │   └── chat.types.ts
│   └── store/
│       └── chatStore.ts
│
├── schedule/
│   ├── components/
│   │   ├── TodoList.tsx              # Todo 목록
│   │   ├── TaskBoard.tsx             # Task 칸반 보드
│   │   └── SprintView.tsx            # 스프린트 뷰
│   ├── hooks/
│   │   ├── useTodo.ts
│   │   ├── useTask.ts
│   │   └── useSprint.ts
│   ├── api/
│   │   └── scheduleApi.ts
│   ├── types/
│   │   └── schedule.types.ts
│   └── store/
│       └── scheduleStore.ts
│
└── ai/
    ├── components/
    │   ├── AIReviewPanel.tsx         # AI 리뷰 패널
    │   └── ReviewResult.tsx          # 리뷰 결과
    ├── hooks/
    │   └── useAIReview.ts
    ├── api/
    │   └── aiApi.ts
    ├── types/
    │   └── ai.types.ts
    └── store/
        └── aiStore.ts
```

**주요 화면:**
1. **IDE 메인 페이지** (`/editor/:projectId`)
   - 왼쪽: 파일 트리
   - 가운데: 코드 에디터 + 탭
   - 오른쪽: 사이드 패널 (채팅/Todo/Task/AI/설정)
   - 하단: 터미널 (기본)

**사용할 백엔드 API:**

**파일/폴더 (BE: 김민규):**
- `GET /api/projects/{projectId}/tree` - 파일 트리
- `GET /api/files/{fileId}` - 파일 내용 로드
- `PUT /api/files/{fileId}` - 파일 내용 저장
- `POST /api/projects/{projectId}/files` - 파일 생성
- `POST /api/projects/{projectId}/folders` - 폴더 생성
- `DELETE /api/files/{fileId}` - 파일 삭제

**채팅 (BE: 방영진):**
- WebSocket: `ws://localhost:8080/ws`
- STOMP 구독: `/topic/project.{projectId}`
- STOMP 전송: `/app/chat.send`
- `GET /api/chat/messages?projectId={id}` - 메시지 이력

**일정 관리 (BE: 양종태):**
- `GET /api/todos` - Todo 목록
- `POST /api/todos` - Todo 생성
- `GET /api/projects/{projectId}/tasks` - Task 목록
- `POST /api/projects/{projectId}/tasks` - Task 생성
- `PATCH /api/tasks/{taskId}/status` - Task 상태 변경

**AI 리뷰 (BE: 양종태):**
- `POST /api/ai/review` - AI 코드 리뷰 요청

**협업 대상:**
- **BE 파트 C (김민규)**: 파일/폴더 트리 + 파일 내용 CRUD
- **BE 파트 B (방영진)**: 채팅 WebSocket 연동
- **BE 파트 D (양종태)**: Todo/Task/AI 리뷰 화면 연동
- **FE 파트 1 (박영선)**: 프로젝트 정보·라우팅 연동

**코드 예시:**
```typescript
// features/editor/components/MonacoEditor.tsx
import { Editor } from '@monaco-editor/react';
import { useEditorStore } from '../store/editorStore';

export const MonacoEditor = () => {
  const { currentFile, content, updateContent } = useEditorStore();

  return (
    <Editor
      height="100%"
      language={currentFile?.language || 'typescript'}
      value={content}
      onChange={(value) => updateContent(value || '')}
      theme="vs-dark"
      options={{
        fontSize: 14,
        minimap: { enabled: true },
        automaticLayout: true,
      }}
    />
  );
};

// features/fileTree/components/FileTree.tsx
import { useFileTreeStore } from '../store/fileTreeStore';

export const FileTree = () => {
  const { tree, openFile } = useFileTreeStore();

  return (
    <div className="file-tree">
      {tree.map(node => (
        <FileNode
          key={node.id}
          node={node}
          onSelect={openFile}
        />
      ))}
    </div>
  );
};

// features/chat/hooks/useWebSocket.ts
import { Client } from '@stomp/stompjs';
import SockJS from 'sockjs-client';

export const useWebSocket = (projectId: string) => {
  const client = new Client({
    brokerURL: 'ws://localhost:8080/ws',
    webSocketFactory: () => new SockJS('http://localhost:8080/ws'),
    onConnect: () => {
      client.subscribe(`/topic/project.${projectId}`, (message) => {
        console.log('Received:', message.body);
      });
    },
  });

  client.activate();
};

// features/schedule/components/TodoList.tsx
import { useTodo } from '../hooks/useTodo';

export const TodoList = () => {
  const { todos, addTodo, toggleTodo } = useTodo();

  return (
    <div className="todo-list">
      {todos.map(todo => (
        <div key={todo.id} className="todo-item">
          <input
            type="checkbox"
            checked={todo.completed}
            onChange={() => toggleTodo(todo.id)}
          />
          <span>{todo.content}</span>
        </div>
      ))}
    </div>
  );
};
```

---

## 7. ⚠️ 주의사항 (반드시 읽기!)

### 🚫 절대 수정하면 안 되는 파일

1. **`main.tsx`** - 앱 진입점
2. **`vite.config.ts`** - Vite 설정
3. **`.env.development`** - 환경 변수 (값만 수정 가능, 키는 수정 금지)
4. **`tsconfig.json`** - TypeScript 설정

### ⚠️ 팀장 승인 필요한 변경사항

1. **`shared/` 폴더 수정 (단, 박영선은 자유 수정 가능)**
   - **박영선**: `shared/` 전체 자유 수정 가능 (공통 컴포넌트 담당)
   - **박유경**: `shared/` 수정 시 박영선에게 확인

2. **의존성 추가 (`package.json`)**
   - 새로운 npm 패키지 설치 시 팀장 승인 필요
   - 예: 새로운 UI 라이브러리, 상태 관리 도구 등

3. **라우팅 변경 (`router/`)**
   - 전체 라우트 구조 변경 시 팀원과 조율 (박영선 담당)

4. **API 클라이언트 수정 (`shared/utils/api.ts`)**
   - Axios 인터셉터, 베이스 URL 등 (박영선 담당)

### ✅ 자유롭게 작업 가능한 영역

1. **자신의 `features/` 디렉토리 내부**
   - **박영선**: `features/auth/`, `features/project/`
   - **박유경**: `features/editor/`, `features/fileTree/`, `features/chat/`, `features/schedule/`, `features/ai/`

2. **박영선 전용**
   - `shared/` 전체 (공통 컴포넌트, 훅, 유틸)
   - 라우팅 설정

3. **자신의 컴포넌트 스타일링**
   - Tailwind CSS 클래스 사용
   - 컴포넌트 내부 CSS

### 📝 코딩 컨벤션

#### 파일 네이밍
- **컴포넌트**: `PascalCase.tsx` (예: `LoginForm.tsx`)
- **훅**: `camelCase.ts`, `use` 접두사 (예: `useAuth.ts`)
- **유틸**: `camelCase.ts` (예: `formatDate.ts`)
- **타입**: `camelCase.types.ts` (예: `auth.types.ts`)
- **API**: `camelCase.api.ts` (예: `authApi.ts`)

#### 컴포넌트 구조
```typescript
// ✅ 좋은 예시
import { useState } from 'react';
import type { LoginFormProps } from '../types/auth.types';

export const LoginForm = ({ onSubmit }: LoginFormProps) => {
  const [email, setEmail] = useState('');
  const [password, setPassword] = useState('');

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();
    onSubmit({ email, password });
  };

  return (
    <form onSubmit={handleSubmit} className="space-y-4">
      <input
        type="email"
        value={email}
        onChange={(e) => setEmail(e.target.value)}
        className="w-full px-4 py-2 border rounded"
      />
      <button type="submit" className="btn-primary">
        로그인
      </button>
    </form>
  );
};
```

#### TypeScript 타입 정의
```typescript
// features/auth/types/auth.types.ts
export interface User {
  id: number;
  email: string;
  name: string;
  createdAt: string;
}

export interface LoginRequest {
  email: string;
  password: string;
}

export interface LoginResponse {
  user: User;
  token: string;
}
```

#### API 호출 패턴
```typescript
// features/auth/hooks/useLogin.ts
import { useMutation } from '@tanstack/react-query';
import { authApi } from '../api/authApi';
import { useAuthStore } from '../store/authStore';

export const useLogin = () => {
  const { login } = useAuthStore();

  return useMutation({
    mutationFn: authApi.login,
    onSuccess: (data) => {
      login(data.user, data.token);
    },
  });
};
```

---

## 8. Git 브랜치 전략

### 브랜치 이름 규칙
```
feature/{기능명}-{세부기능}

예시:
- feature/auth-login-ui (박영선)
- feature/project-list-ui (박영선)
- feature/editor-monaco (박유경)
- feature/filetree-ui (박유경)
- feature/chat-websocket (박유경)
```

### 작업 순서
```bash
# 1. develop 브랜치에서 최신 코드 받기
git checkout develop
git pull origin develop

# 2. 기능 브랜치 생성
git checkout -b feature/auth-login-ui

# 3. 작업 후 커밋
git add .
git commit -m "feat(auth): 로그인 폼 UI 구현"

# 4. Push
git push origin feature/auth-login-ui

# 5. GitHub에서 Pull Request 생성
# develop <- feature/auth-login-ui
```

### 커밋 메시지 컨벤션
```
feat(기능명): 기능 설명
fix(기능명): 버그 수정 설명
style(기능명): 스타일 변경
refactor(기능명): 리팩토링

예시:
feat(auth): 로그인 폼 UI 구현
fix(editor): 코드 저장 시 깜빡임 수정
style(fileTree): 파일 아이콘 색상 변경
refactor(chat): 메시지 컴포넌트 분리
```

---

## 9. 백엔드 연동

### API 베이스 URL 설정

**`.env.development`** (로컬 개발):
```bash
VITE_API_BASE_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080/ws
```

**`.env.production`** (프로덕션):
```bash
VITE_API_BASE_URL=https://api.editus.com
VITE_WS_URL=wss://api.editus.com/ws
```

### API 클라이언트 설정 (박영선 담당)

```typescript
// shared/utils/api.ts
import axios from 'axios';

export const apiClient = axios.create({
  baseURL: import.meta.env.VITE_API_BASE_URL,
  headers: {
    'Content-Type': 'application/json',
  },
});

// 요청 인터셉터: JWT 토큰 자동 추가
apiClient.interceptors.request.use((config) => {
  const token = localStorage.getItem('token');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 응답 인터셉터: 에러 처리
apiClient.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      // 토큰 만료 -> 로그아웃 처리
      localStorage.removeItem('token');
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);
```

### 백엔드 실행 확인

프론트엔드 개발 시 **백엔드가 실행 중이어야** API 호출이 가능합니다.

**백엔드 실행 여부 확인:**
```bash
curl http://localhost:8080
```
- 응답이 오면 백엔드 실행 중
- `Connection refused` 에러 나면 백엔드 미실행

**백엔드 팀원에게 요청:**
- "백엔드 실행해주세요!"

---

## 10. 문제 해결

### 🔴 문제 1: npm install 실패
**에러:**
```
EACCES: permission denied
```

**해결:**
```bash
npm cache clean --force
npm install
```

---

### 🔴 문제 2: Vite 개발 서버 실행 안 됨
**에러:**
```
Port 5173 is already in use
```

**해결:**
1. 기존 프로세스 종료
2. 또는 다른 포트 사용: `npm run dev -- --port 3000`

---

### 🔴 문제 3: API 호출 실패 (CORS 에러)
**에러:**
```
Access to XMLHttpRequest has been blocked by CORS policy
```

**원인:**
- 백엔드가 실행 중이 아님
- 백엔드 CORS 설정 문제

**해결:**
1. 백엔드 실행 확인: `curl http://localhost:8080`
2. 백엔드 팀원(이세종)에게 CORS 설정 요청

---

### 🔴 문제 4: 타입 에러
**에러:**
```
Property 'xxx' does not exist on type 'YYY'
```

**해결:**
1. `types/` 폴더에서 타입 정의 확인
2. 타입 정의가 없으면 생성
3. VSCode 재시작

---

### 🔴 문제 5: Hot Reload 안 됨
**증상:**
- 코드 수정해도 브라우저 자동 갱신 안 됨

**해결:**
1. Vite 개발 서버 재시작 (`Ctrl+C` 후 `npm run dev`)
2. 브라우저 캐시 삭제 (`Cmd+Shift+R` 또는 `Ctrl+Shift+R`)

---

## 11. 유용한 VSCode 확장

- **ES7+ React/Redux/React-Native snippets** - React 코드 스니펫
- **Tailwind CSS IntelliSense** - Tailwind 자동완성
- **ESLint** - 코드 품질 체크
- **Prettier** - 코드 포맷팅
- **Auto Rename Tag** - HTML 태그 자동 수정
- **Path Intellisense** - 경로 자동완성

---

## 12. 빠른 시작 체크리스트

- [ ] Node.js 설치 확인 (`node -v`)
- [ ] 프로젝트 클론
- [ ] `cd frontend`
- [ ] `npm install`
- [ ] `npm run dev`
- [ ] 브라우저에서 `http://localhost:5173` 접속
- [ ] 백엔드 실행 확인 (`curl http://localhost:8080`)
- [ ] 본인 담당 기능 폴더에서 작업 시작

---

## 📞 문제 발생 시

1. 이 가이드의 [문제 해결](#10-문제-해결) 섹션 확인
2. 팀 채팅방에 에러 메시지 캡처해서 공유
3. 브라우저 콘솔(F12) 에러 로그 공유
4. 백엔드 연동 문제는 백엔드 팀원에게 확인

---

**작성일**: 2024-12-10
**작성자**: EditUs Frontend Team
**버전**: 2.0 (역할 분담 수정)