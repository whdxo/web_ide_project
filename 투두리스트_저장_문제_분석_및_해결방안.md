# 투두리스트 저장 문제 분석 및 해결 방안

## 📌 문제 요약

**증상**: 투두리스트에서 할일을 작성하고 저장을 눌러도 저장이 안됨
**환경**: 로컬에서는 정상 동작, 배포/도커 환경에서는 저장 실패

---

## 🔍 원인 분석

### 핵심 문제: 하드코딩된 localhost URL

**파일**: `frontend/src/features/schedule/api/todoApi.ts:1`

```typescript
const API_BASE_URL = 'http://localhost:8080/api';  // ❌ 문제!
```

**문제점**:
1. ❌ **배포 환경에서 작동 불가**: 프론트엔드가 도커 컨테이너에서 실행되면 `localhost:8080`은 **자기 자신의 컨테이너**를 가리킴
2. ❌ **백엔드 서버에 접근 불가**: 백엔드는 다른 컨테이너 또는 다른 서버에서 실행 중
3. ❌ **네트워크 에러 발생**: `ERR_CONNECTION_REFUSED` 또는 `CORS Error`

### 다른 API 파일과의 비교

#### ✅ 올바른 예시 (fileApi.ts, projectApi.ts 등)

```typescript
import { apiClient } from './client';

export const fileApi = {
  getFileContent: async (fileId: number) => {
    const response = await apiClient.get(`/api/files/${fileId}`);
    return response.data;
  },
};
```

**apiClient 장점**:
- ✅ 환경 변수로 BASE_URL 자동 설정
- ✅ 로컬: `http://localhost:8080`
- ✅ 배포: 환경 변수 `VITE_API_URL` 사용
- ✅ JWT 토큰 자동 포함
- ✅ 에러 핸들링 통합

#### ❌ 문제 있는 예시 (todoApi.ts)

```typescript
const API_BASE_URL = 'http://localhost:8080/api';  // ❌ 하드코딩

export const todoApi = {
  createTodo: async (request) => {
    const response = await fetch(`${API_BASE_URL}/todos`, {  // ❌ raw fetch
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },  // ❌ JWT 토큰 없음
      body: JSON.stringify(request),
    });
    return data.data;
  },
};
```

**문제점**:
1. ❌ URL 하드코딩 → 배포 환경에서 작동 불가
2. ❌ JWT 토큰 헤더 없음 → 인증 실패 가능
3. ❌ raw `fetch` 사용 → 에러 핸들링 없음

---

## 🐛 증상 상세 분석

### 로컬 환경 (정상 동작)

```
프론트엔드 (localhost:5173)
  ↓
  fetch('http://localhost:8080/api/todos')
  ↓
백엔드 (localhost:8080)
  ↓
DB에 저장 ✅
```

### 배포/도커 환경 (저장 실패)

```
프론트엔드 (도커 컨테이너 A)
  ↓
  fetch('http://localhost:8080/api/todos')  ← localhost는 자기 자신!
  ↓
❌ 컨테이너 A 내부의 8080 포트 (백엔드 없음)
  ↓
❌ Connection Refused
```

**실제 구조**:
```
프론트엔드 컨테이너 (port 3000)
백엔드 컨테이너   (port 8080)  ← 다른 컨테이너!
```

프론트엔드에서 백엔드로 접근하려면:
- ❌ `http://localhost:8080` (자기 자신)
- ✅ `http://backend:8080` (Docker 네트워크)
- ✅ `https://api.yourdomain.com` (프로덕션)

---

## 🛠️ 해결 방안

### 방안 1: todoApi.ts를 apiClient 방식으로 전환 (권장 ⭐)

#### 1-1. todoApi.ts 수정

**Before**:
```typescript
const API_BASE_URL = 'http://localhost:8080/api';

export const todoApi = {
  async getTodos(params) {
    const response = await fetch(`${API_BASE_URL}/todos?...`);
    const data = await response.json();
    return data.data;
  },

  async createTodo(request) {
    const response = await fetch(`${API_BASE_URL}/todos`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(request),
    });
    const data = await response.json();
    return data.data;
  },

  // ... 나머지 메서드
};
```

**After**:
```typescript
import { apiClient } from '@/shared/api/client';

export const todoApi = {
  async getTodos(params) {
    const response = await apiClient.get('/api/todos', { params });
    return response.data.data;
  },

  async createTodo(request) {
    const response = await apiClient.post('/api/todos', request);
    return response.data.data;
  },

  async updateTodo(todoId, request) {
    const response = await apiClient.put(`/api/todos/${todoId}`, request);
    return response.data.data;
  },

  async toggleTodo(todoId) {
    const response = await apiClient.patch(`/api/todos/${todoId}/toggle`);
    return response.data.data;
  },

  async deleteTodo(todoId) {
    await apiClient.delete(`/api/todos/${todoId}`);
  },
};
```

#### 1-2. 장점

- ✅ 환경에 따라 자동으로 API URL 설정
- ✅ JWT 토큰 자동 포함
- ✅ 에러 핸들링 통합
- ✅ 다른 API 파일들과 일관성
- ✅ 인터셉터 활용 (토큰 갱신, 로깅 등)

---

### 방안 2: 환경 변수 사용 (비권장, 임시방편)

```typescript
const API_BASE_URL = import.meta.env.VITE_API_URL || 'http://localhost:8080/api';

export const todoApi = {
  async createTodo(request) {
    const response = await fetch(`${API_BASE_URL}/todos`, {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${localStorage.getItem('token')}`,  // JWT 추가
      },
      body: JSON.stringify(request),
    });
    return data.data;
  },
};
```

**`.env` 파일**:
```bash
# 로컬
VITE_API_URL=http://localhost:8080/api

# 배포
VITE_API_URL=https://api.yourdomain.com
```

**문제점**:
- ❌ 여전히 raw fetch 사용
- ❌ JWT 토큰 수동 관리
- ❌ 에러 핸들링 없음
- ❌ 다른 API 파일과 일관성 없음

---

## 📋 작업 순서

### Phase 1: todoApi.ts 수정 (15분)

1. ✅ `apiClient` import 추가
2. ✅ `API_BASE_URL` 제거
3. ✅ `fetch` 호출을 `apiClient` 메서드로 변경
   - `fetch()` → `apiClient.get/post/put/patch/delete()`
4. ✅ 타입 정의 확인 및 정리

### Phase 2: 테스트 (10분)

1. ✅ 로컬 환경에서 투두 CRUD 동작 확인
   - 생성, 조회, 수정, 삭제, 완료 토글
2. ✅ 개발자 도구 Network 탭에서 API 호출 확인
   - Authorization 헤더 포함 여부
   - 응답 상태 코드

### Phase 3: 배포 테스트 (선택)

1. ✅ `.env` 파일에 배포용 API URL 설정
2. ✅ 빌드 및 도커 실행
3. ✅ 투두 저장 기능 테스트

---

## 🔑 핵심 포인트

### 현재 상황
```
todoApi.ts: ❌ localhost:8080 하드코딩
다른 API:   ✅ apiClient 사용

결과: 로컬 OK, 배포 ❌
```

### 수정 후
```
todoApi.ts: ✅ apiClient 사용
다른 API:   ✅ apiClient 사용

결과: 로컬 OK, 배포 ✅
```

---

## 📊 비교표

| 항목 | 현재 (todoApi.ts) | 다른 API (fileApi.ts 등) |
|------|-------------------|--------------------------|
| URL 관리 | ❌ 하드코딩 localhost | ✅ 환경 변수 자동 |
| JWT 토큰 | ❌ 없음 | ✅ 자동 포함 |
| 에러 핸들링 | ❌ 없음 | ✅ 인터셉터 활용 |
| 로컬 동작 | ✅ 정상 | ✅ 정상 |
| 배포 동작 | ❌ 실패 | ✅ 정상 |

---

## ✅ 체크리스트

**todoApi.ts 수정 완료 시**:
- [ ] `apiClient` import 추가
- [ ] `API_BASE_URL` 상수 제거
- [ ] `fetch()` 호출을 `apiClient` 메서드로 변경
- [ ] JWT 헤더 수동 추가 코드 제거 (apiClient가 자동 처리)
- [ ] 로컬 환경에서 투두 생성 테스트
- [ ] 로컬 환경에서 투두 수정 테스트
- [ ] 로컬 환경에서 투두 삭제 테스트
- [ ] 개발자 도구에서 Authorization 헤더 확인

---

## 🎯 결론

**문제**: `todoApi.ts`가 `localhost:8080`을 하드코딩하여 배포 환경에서 작동 불가

**해결**: `apiClient`를 사용하도록 변경

**장점**:
1. ✅ 로컬/배포 환경 모두 정상 동작
2. ✅ JWT 인증 자동 처리
3. ✅ 에러 핸들링 통합
4. ✅ 코드 일관성 향상

**작업량**: 약 15분 (파일 1개 수정)
