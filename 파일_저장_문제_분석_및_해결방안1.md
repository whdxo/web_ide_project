# 파일 저장 문제 분석 및 해결 방안

## 📌 문제 요약

**증상**: 도커 환경에서 코드 저장 시 에러 발생
**원인**: 백엔드는 S3 전용으로 전환되었으나, 프론트엔드는 예전 방식(서버 직접 통신)을 사용 중

---

## 🔍 상세 분석

### 1. 백엔드 현황

#### ✅ S3 저장 방식 (새로운 방식 - 구현 완료)
**위치**: `FileContentController.java` + `FileContentService.java`

```java
// 1단계: 업로드 URL 발급
POST /api/files/{fileId}/upload-url
→ S3 Presigned URL 반환 (3분 유효)

// 2단계: 클라이언트가 S3에 직접 업로드
PUT <presigned-url>
Content-Type: text/plain
Body: 파일 내용

// 3단계: 다운로드 시
GET /api/files/{fileId}/content-url
→ S3 Presigned URL 반환 (3분 유효)

GET <presigned-url>
→ S3에서 직접 다운로드
```

**장점**:
- ✅ 도커 컨테이너는 stateless (영구 스토리지 불필요)
- ✅ 서버 부하 감소 (파일 전송을 S3가 처리)
- ✅ 확장성 좋음

#### ❌ 예전 방식 (현재 비활성화됨)
**위치**: `FileTreeService.java:86-93`

```java
@Transactional
public FileResponse updateFileContent(Long fileId, UpdateFileContentRequest req) {
    throw new UnsupportedOperationException(
        "파일 내용 저장은 S3 업로드 방식으로 처리합니다. " +
        "/api/files/{id}/upload-url 를 사용하세요."
    );
}
```

**중요**: `PUT /api/files/{fileId}` 엔드포인트는 **무조건 에러를 던지도록 변경됨**

---

### 2. 프론트엔드 현황

#### ❌ 현재 사용 중 (예전 방식)
**위치**: `fileApi.ts:79-88`

```typescript
// 파일 내용 저장 - 예전 방식 사용 중
saveFileContent: async (fileId: number, data: SaveFileRequest): Promise<SaveFileResponse> => {
  const response = await apiClient.put<SaveFileResponse>(
    `/api/files/${fileId}`,  // ← 이 엔드포인트는 이제 에러를 던짐!
    data
  );
  return response.data;
},
```

**사용 위치**:
- `useFileContent.ts:19-31` - useSaveFile 훅
- `useAutoSave.ts:5-30` - 자동 저장 (30초마다)
- `MonacoEditor.tsx` - Ctrl+S 저장

**문제점**:
```
프론트엔드 → PUT /api/files/{fileId}
            ↓
백엔드      → throw UnsupportedOperationException()
            ↓
결과        → 저장 실패 에러
```

---

### 3. 도커 환경에서 문제가 되는 이유

```yaml
# application-dev.yml (도커 환경에서 사용)
aws:
  s3:
    bucket: editus-dev-bucket
    region: ap-northeast-2
```

1. **도커 컨테이너**는 재시작 시 데이터 손실 (ephemeral storage)
2. **S3 사용이 필수**이지만, 프론트엔드가 예전 API를 호출
3. **백엔드가 예전 API를 막아놨음** (UnsupportedOperationException)
4. **결과**: 파일 저장 시 100% 에러 발생

---

## 🛠️ 해결 방안

### 방안 1: 프론트엔드를 S3 방식으로 전환 (권장 ⭐)

#### 1-1. fileApi.ts 수정

```typescript
// AS-IS (예전 방식)
saveFileContent: async (fileId: number, data: SaveFileRequest) => {
  const response = await apiClient.put(`/api/files/${fileId}`, data);
  return response.data;
},

// TO-BE (S3 방식)
saveFileContent: async (fileId: number, content: string) => {
  // 1단계: 업로드 URL 발급
  const uploadUrlResponse = await apiClient.post<{uploadUrl: string, contentKey: string}>(
    `/api/files/${fileId}/upload-url`
  );

  // 2단계: S3에 직접 업로드
  await fetch(uploadUrlResponse.data.uploadUrl, {
    method: 'PUT',
    headers: {
      'Content-Type': 'text/plain',
    },
    body: content,
  });

  return { success: true };
},

// 파일 내용 조회도 수정 필요
getFileContent: async (fileId: number) => {
  // 1단계: 다운로드 URL 발급
  const downloadUrlResponse = await apiClient.get<{downloadUrl: string}>(
    `/api/files/${fileId}/content-url`
  );

  // 2단계: S3에서 직접 다운로드
  const response = await fetch(downloadUrlResponse.data.downloadUrl);
  const content = await response.text();

  return {
    data: {
      id: fileId,
      content: content,
    }
  };
},
```

#### 1-2. 관련 타입 정의 추가

**파일**: `frontend/src/shared/features-types/file.types.ts`

```typescript
// 추가
export interface UploadUrlResponse {
  uploadUrl: string;
  contentKey: string;
}

export interface DownloadUrlResponse {
  downloadUrl: string;
}
```

#### 1-3. 수정 필요 파일 목록

```
frontend/src/
├── shared/
│   ├── api/fileApi.ts                     [수정] S3 방식으로 변경
│   └── features-types/file.types.ts       [수정] 타입 추가
├── features/editor/
│   ├── hooks/
│   │   ├── useFileContent.ts              [확인] 타입 체크
│   │   └── useAutoSave.ts                 [확인] 동작 확인
│   └── components/
│       └── MonacoEditor.tsx               [확인] 저장 기능 테스트
```

#### 1-4. 예상 변경 사항

**Before**:
```
사용자가 Ctrl+S 누름
  ↓
프론트엔드: PUT /api/files/{fileId} { content: "..." }
  ↓
백엔드: ❌ UnsupportedOperationException
```

**After**:
```
사용자가 Ctrl+S 누름
  ↓
프론트엔드: POST /api/files/{fileId}/upload-url
  ↓
백엔드: ✅ Presigned URL 반환
  ↓
프론트엔드: PUT <S3 Presigned URL> (직접 S3에 업로드)
  ↓
S3: ✅ 파일 저장 완료
```

---

### 방안 2: 백엔드에 호환성 엔드포인트 추가 (임시방편, 비권장)

**이유**: S3 방식으로 이미 전환된 상태에서 다시 예전 방식을 지원하면 아키텍처가 혼란스러워짐

만약 긴급하게 필요하다면:

```java
// FileTreeService.java
@Transactional
public FileResponse updateFileContent(Long fileId, UpdateFileContentRequest req) {
    IdeFile file = ideFileRepository.findById(fileId)
        .orElseThrow(() -> new IllegalArgumentException("파일이 존재하지 않습니다."));

    // DB의 content 컬럼에 직접 저장 (임시방편)
    file.setContent(req.getContent());

    // WARNING: 도커 환경에서는 DB 외부 저장소 사용 불가!
    return FileResponse.from(file);
}
```

**문제점**:
- ❌ 대용량 파일 시 DB 부하
- ❌ content를 DB에 저장하면 S3 아키텍처와 불일치
- ❌ 장기적으로 기술 부채

---

## 📋 권장 작업 순서

### Phase 1: 프론트엔드 수정 (1-2시간)

1. ✅ `fileApi.ts` - S3 방식으로 저장/조회 로직 변경
2. ✅ `file.types.ts` - 타입 정의 추가
3. ✅ 테스트: 로컬 환경에서 파일 저장/조회 동작 확인

### Phase 2: 통합 테스트 (30분)

1. ✅ 파일 생성 → 내용 입력 → 저장 (Ctrl+S)
2. ✅ 자동 저장 동작 확인 (30초)
3. ✅ 파일 재로드 시 내용 유지 확인
4. ✅ 다른 브라우저에서 동일 파일 열어서 내용 확인

### Phase 3: 도커 환경 테스트 (30분)

1. ✅ 도커 빌드 및 실행
2. ✅ S3 버킷 권한 확인
3. ✅ 파일 저장 → S3 버킷에서 확인
4. ✅ 컨테이너 재시작 후에도 파일 내용 유지 확인

---

## 🔑 핵심 포인트

### 현재 상황
```
백엔드: ✅ S3 전용으로 전환 완료
프론트: ❌ 예전 방식 사용 중
결과:   ❌ 저장 에러 발생
```

### 해결 후
```
백엔드: ✅ S3 Presigned URL 제공
프론트: ✅ S3 직접 업로드/다운로드
결과:   ✅ 파일 저장 정상 동작
```

---

## 📝 참고사항

### S3 Presigned URL이란?

```
일반 S3 접근:
사용자 → 서버 → S3 (서버가 중계)
문제: 서버 부하, 네트워크 2배

Presigned URL 방식:
사용자 → 서버 (URL 발급만)
사용자 → S3 (직접 업로드/다운로드)
장점: 서버 부하 없음, 빠름, 확장성 좋음
```

### 보안
- ✅ Presigned URL은 3분간만 유효
- ✅ 특정 파일에만 접근 가능
- ✅ JWT 인증을 통과한 사용자만 URL 발급 가능

### 비용
- ✅ S3 PUT/GET 요청 비용 (매우 저렴)
- ✅ 데이터 전송 비용 (아웃바운드만)
- ✅ 서버 트래픽 감소로 EC2 비용 절감

---

## ✅ 체크리스트

**프론트엔드 수정 완료 시**:
- [ ] fileApi.ts - saveFileContent를 S3 방식으로 변경
- [ ] fileApi.ts - getFileContent를 S3 방식으로 변경
- [ ] file.types.ts - UploadUrlResponse, DownloadUrlResponse 타입 추가
- [ ] 로컬 환경에서 저장/조회 테스트
- [ ] 자동 저장 동작 확인
- [ ] MonacoEditor에서 Ctrl+S 테스트
- [ ] 에러 핸들링 추가 (네트워크 실패, URL 만료 등)

**백엔드 확인사항**:
- [x] S3Config.java 설정 완료
- [x] S3PresignService.java 구현 완료
- [x] FileContentController.java 엔드포인트 존재
- [x] FileContentService.java 로직 구현 완료
- [ ] AWS S3 버킷 생성 및 권한 설정
- [ ] IAM Role 또는 Access Key 설정

---

## 🎯 결론

**핵심**: 프론트엔드를 S3 방식으로 변경하면 모든 문제 해결

**장점**:
1. ✅ 도커 환경에서 정상 동작
2. ✅ 서버 부하 감소
3. ✅ 확장성 향상
4. ✅ 아키텍처 일관성

**작업량**: 프론트엔드 파일 2-3개 수정 (1-2시간)
